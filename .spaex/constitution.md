<!-- spaex-composed:source_hash="991ffc62003416bcd78b9c9a73193fb06f9c8744ec61776b318f375a23a9d1f4" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- A file that is clearly over 500 LoC must be split or have an explicit, documented reason to remain large together with a concrete plan for a later split. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before the first edit, verify the repository root, the active branch, and the registered worktrees, and if no suitable worktree exists, create one before modifying files. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any existing Git repository, all changes must be authored in a dedicated repository-local worktree (a directory registered for the target repository by `git worktree list`) on a topic branch; this applies to source code, tests, documentation, configuration, and generated files that are part of the change, and a copied directory or an unrelated clone does not satisfy this requirement. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test bodies and test-only helpers must live in dedicated test files, not inline in production files. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test code must be maintained in files separate from production code in every language, including unit, integration, contract, and end-to-end tests. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- On a tracked branch, bootstrap the graph with `graphify update <repo-root>` before authoring if `graphify-out/graph.json` is absent, and refresh it the same way if `graphify-out/.meta.json` is missing, invalid, or its `indexed_at_sha` does not match current `HEAD`; on a linked worktree or feature branch, use the complete fork-point snapshot as-is without comparing it to the advancing `HEAD` or refreshing it — these checks hold even when the `post-commit`/`post-checkout` hooks are not installed or were bypassed. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- When the graph query finds an identical or near-identical candidate, propose extending it by citing its `file:line` location, the proposed extended signature, the estimated lines saved, and one concrete call-site rewritten as proof-of-concept, then wait for operator approval before making further changes; when the match is borderline or extending the candidate risks scope creep, stop and ask the operator rather than deciding autonomously. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Before authoring any new named function, class, component, store, module, or CLI command (not renames, in-place edits, trivial inline expressions, or code being deleted), check whether it already exists — reuse or extend an existing helper, util, pattern, or graph candidate rather than authoring a parallel implementation; for a new named artifact, perform this check by querying the project's `graphify` knowledge graph (`graphify query`, `graphify path`, `graphify explain`) and evaluating every candidate returned, including candidates that are unexported, incomplete, or unreachable from public surfaces. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`, `com.github.haexmas.atoms.ponytail/ponytail`]_
- Leave exactly one runnable check behind any non-trivial logic change, written with the project's existing test or assertion tooling, or, only for a project with none yet, a minimal framework-free assert-based demo or single test file; trivial one-liners need no check. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark any deliberate simplification that cuts a real corner with a known ceiling, such as a global lock, an O(n²) scan, or a naive heuristic, with a `ponytail:` comment naming the ceiling and the upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When fixing a reported bug, find the root cause rather than the symptom: grep every caller of the function being touched and fix the shared function once instead of patching only the call site the report named. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MUST_NOT

- Do not assume that a platform-specific global or API exists everywhere, since browser, Node.js, and serverless environments have different APIs and failure modes. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not introduce a barrel export or a new abstraction only to shorten an import path. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not leave promises unhandled. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not replace repository tooling with a personal global installation. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not use `||` when a valid value such as `0`, `false`, or `""` must be preserved, and do not hide a required value behind optional chaining without deciding how absence is handled. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not use mutation or shared mutable module state as an accidental communication channel. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Do not discard or obscure the original error: do not catch an error merely to log and rethrow it unchanged, which usually duplicates logs and loses ownership of the failure decision, and do not replace it with an uninformative string. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`, `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not assume that a test file is discovered unless the runner configuration confirms it. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not leave floating promises in tests, and do not let a test pass before the asynchronous work it is meant to verify has completed. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not mock the unit's own logic so thoroughly that the test only verifies the mock wiring. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not turn every test into a slow end-to-end test. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Do not depend on real network calls or services, shared mutable state, test order, wall-clock timing, random values without a controlled seed, or a developer-specific filesystem, and do not use arbitrary sleeps to wait for time or tasks to pass. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Do not add `.clone()` merely to silence the borrow checker. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not encode a closed set of states or an optional value as sentinel values or loosely related booleans. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not introduce a new crate, abstraction, or async strategy without a concrete problem that the existing code cannot solve. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not perform blocking file, process, or CPU-heavy work directly on an async executor thread, and do not create a new runtime inside an already-running async context. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not use `unwrap()` or `expect()` on data, I/O, configuration, or network input. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Do not chase a coverage percentage at the expense of meaningful assertions. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Keep the primary checkout reserved for inspection and worktree management; do not author changes there (read-only inspection of any checkout is fine). _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- LLM agents must not leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts, including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, metadata, and other repository content. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Never add artificial wrappers, blank lines, or extra files only to satisfy a line-count threshold; a short but tangled module is not healthy, and a long but cohesive module is not automatically wrong. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- A failed, timed-out, or garbage-returning `graphify update`, `query`, `path`, or `explain` invocation must not block authoring; instead warn by naming the failed invocation and its outcome, proceed with authoring, and flag the skipped or incomplete consultation for a later manual check. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Do not introduce a speculative abstraction, or an abstraction added solely because a change might be reusable; a concrete, domain-owned abstraction is not premature and may still be used, kept as small as possible, when it improves cohesion, removes real duplication, or makes a branch independently testable. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD

- Follow the existing runtime and module-system conventions. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Keep asynchronous control flow explicit: await promises whose result or failure matters, handle errors at the boundary that can recover or report them, and use `Promise.all` only when operations are independent and fail-fast behavior is acceptable. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Keep modules and exports focused; prefer named exports for reusable library code. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Keep untyped escape hatches local and explain why they are necessary. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Prefer `const` and narrow local mutation; use `let` when reassignment makes the state transition clearer. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Prefer immutable transformations at shared boundaries, but allow simple local mutation when it is clearer and measurably avoids needless allocation, since readability and correct ownership of state matter more than a blanket immutability rule. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Treat TypeScript's `strict` mode as the baseline for new or actively maintained TypeScript code. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Use `===`/`!==`, `??`, and `?.` deliberately. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Validate data at trust boundaries — HTTP responses, request bodies, files, environment variables, message queues, and browser storage — since a TypeScript type assertion does not make them safe and has no runtime effect; parse or validate the data once, then pass the resulting domain type inward. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Add useful context to an error at a boundary where it helps the caller understand what failed, while preserving the underlying error cause when translating or propagating it. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`, `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Use the project's configured formatter, linter, compiler, and test runner before handing off a change — for TypeScript at minimum `tsc --noEmit` (or the project's equivalent), linting, formatting checks, and the relevant tests should pass; for Rust, run `cargo fmt --check`, `cargo check`, `cargo clippy`, and `cargo test` when the project supports those commands. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`, `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Await every promise that affects the assertion, and use `Promise.all` or `Promise.allSettled` when the test intentionally exercises concurrent work and its failure semantics. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Choose the narrowest test level that proves the behavior: unit tests for pure logic, integration tests for module boundaries and persistence, and end-to-end tests for critical user journeys. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- For TypeScript, keep type-level tests separate from runtime tests when the project needs to verify inference, assignability, or declaration output, since runtime tests cannot prove a compile-time contract and the compiler cannot prove runtime validation of external data. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Keep mocks narrow and behavior-oriented; mock an external boundary when isolation or determinism requires it. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Prefer a small number of clear assertions per scenario over a large fixture that hides what the test is proving; test names should describe the behavior and relevant condition, not the implementation method name alone. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`]_
- Control time, randomness, environment, and external clients — such as clocks, ports, and other dependencies — at explicit boundaries, injecting them where the behavior needs control. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Keep tests deterministic and isolated. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Test both successful and failure behavior deliberately, including malformed or empty input, missing values or relevant limits, rejected promises or cancellation, timeout, and concurrency behavior, and authorization or persistence failures where they matter. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Test observable behavior and public contracts — not the exact arrangement of private functions or implementation details — so a test fails when the promised behavior regresses and a refactor that preserves the contract does not require rewriting the test suite. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Use the repository's configured runner, environment, formatter, linter, and compiler; a change should include relevant tests and pass the project's normal test command — for Rust, `cargo test` for the complete package or workspace, including unit, integration, and library documentation tests by default — following any project-specific feature, target, or test-runner requirements. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript-testing`, `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Choose the smallest correct implementation that fits the existing crate conventions. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Keep async code non-blocking; use the runtime's blocking facility or a dedicated worker where appropriate. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Keep public APIs narrow, and document public items when the crate's lint policy requires it. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Keep unsafe code isolated and justified: prefer a safe abstraction around a small unsafe block, document the safety argument, and add a test that would expose a violation of the assumed invariant. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Prefer iterators and combinators when they make the transformation clearer, but use an ordinary loop when it communicates control flow or error handling better; idiomatic syntax is not a reason to make code harder to read. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Respect ownership instead of fighting it: borrow when the callee does not need ownership, and return owned data when the result must outlive the input. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Respect the repository's configured Clippy lint levels, enabling stricter categories or `-D warnings` only when the project deliberately requires them, and follow documented command variants when they differ. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Use `Result<T, E>` for recoverable failures and propagate errors with `?`. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- A test suite is production code: keep it readable, fast enough to run often, and trustworthy when it passes. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- For async code, use the async test support of the runtime already used by the project, and test cancellation, timeout, and ordering assumptions when they affect correctness. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Keep test helpers small and local to the tests that own them; a shared fixture is justified when it expresses a stable domain setup, not merely to remove a few repeated lines. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Prefer explicit assertions with useful failure context. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Put focused unit-test implementations in dedicated files separate from production code; use integration tests under `tests/` to exercise the public API as an external consumer would, and use documentation tests for public examples that should compile and remain correct. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- A file should have one primary reason to change, and should be split when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Add or update tests with behavior changes, testing observable behavior and important failure modes rather than private implementation details, since a test is useful only when it can fail for a real regression. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before handoff, run the repository's formatter, static checks, and relevant tests, and report what was run and any checks that were unavailable or skipped. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments should explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, and should be updated when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Extract a helper when it gives a meaningful name to a concept, makes a branch independently testable, or removes repeated logic, not merely because the function crossed an arbitrary line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Follow the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Generated files, vendored code, snapshots, and other files not normally edited by hand should be excluded from the 500 LoC assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Give each function one coherent responsibility at one level of abstraction. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep dependencies directed and understandable, putting I/O, framework adapters, and process wiring at the edges when the architecture calls for such a separation. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each change self-contained and easy to review so that production code, relevant tests, documentation, and configuration belong together when they describe the same behavior, and split independent refactors and unrelated cleanups into separate changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep each task isolated in its own worktree and topic branch so that changes can be reviewed, tested, committed, and removed independently. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep public module interfaces narrow and intentional, hiding implementation details behind the smallest stable API the callers need. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep related tests close to the behavior they verify, according to the language and repository convention; a module split is not complete if its tests, documentation, fixtures, and call sites are left misleadingly behind. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Keep side effects at visible boundaries; pure calculations and decisions should be easy to test without a database, network, filesystem, clock, or global mutable state. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Name code after its domain meaning and behavior; a clear name is better than a comment that explains an otherwise opaque `data`, `helper`, or `process` function. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Optimize for code that another developer can understand, verify, change, and remove safely. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Organize code around cohesive domain or feature boundaries where the codebase supports it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a change that clearly improves maintainability and correctness over theoretical perfection; when a design is uncertain, make the smallest reversible change that provides evidence about the next decision. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer a small amount of local duplication over a premature generic abstraction that couples unrelated callers. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Prefer early returns or named helpers when they make the main path easier to follow, and reduce nesting. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Refactor repeated behavior when the shared rule is real and the abstraction makes both callers clearer, keeping the abstraction close to the domain that owns it. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Separate broad formatting or mechanical refactors from behavior changes when possible, since mixed diffs make review, debugging, rollback, and blame less reliable. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat a change of about 100 net lines as a useful review-size warning and a change approaching 1000 lines as a strong prompt to split the work or agree on the larger change in advance; generated code, mechanical renames, and required migrations are legitimate exceptions, but should be identified as such. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Use smaller soft thresholds, such as a function or method beyond roughly 40 to 60 lines or nesting deeper than roughly three levels, as prompts for review rather than automatic failures, since these thresholds are heuristics, not targets or mandatory limits. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- When a repository already has a documented structure, naming scheme, or size convention, follow it unless the task explicitly changes that convention, since this atom supplies defaults, not a reason to reformat or reorganize an entire codebase. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any question about the codebase — what something does, where it lives, how pieces relate, whether an existing artifact already satisfies a request, or how to trace a symptom to its source, including exploratory lookups incidental to a larger task — run a bounded `graphify query "<intent>" --budget <N>`, or `graphify path`/`graphify explain` for one specific relationship, before a raw `grep`/`rg` search or broad file reads, starting the budget around 800–1500 tokens and increasing it only as needed; then read only the smallest source slices the result points to, and narrow the query or issue one more focused query rather than dumping the repository if the result is truncated or ambiguous. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_
- Before writing new code, work through the laziness ladder in order — whether it needs to be built at all, whether the standard library, a native platform feature, or an already-installed dependency already covers it, or whether it can be reduced to one line — and stop at the first rung that holds, but only after reading the task and the code it touches and tracing the real flow end to end; this does not relax input validation at trust boundaries, error handling that prevents data loss, security, accessibility, necessary hardware or platform calibration, or anything explicitly requested. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer deletion over addition, boring solutions over clever ones, and the fewest files possible; once the problem and the existing flow are understood, the shortest working diff wins, since a small change in the wrong place is a second bug, not a lazy fix. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests by asking whether the functionality is actually needed or an existing piece already covers it, and when two same-sized standard-library approaches are otherwise equal, pick the edge-case-correct one rather than the flimsier one. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Avoid circular dependencies. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Avoid using `any`; use a precise type, a generic, or `unknown` followed by explicit narrowing instead. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- Avoid exposing implementation details solely to work around a local borrow or lifetime problem. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- Avoid weakening a production invariant merely to make a test convenient; make the fixture satisfy the same contract as a real caller. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- Avoid circular imports, hidden global state, and a domain layer that knows about infrastructure details. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Avoid letting a generic utility module become a dumping ground. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Avoid re-exporting everything or creating pass-through modules that add no boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not abstract two things merely because they look similar; first establish that they have the same behavior, ownership, change reason, and invariants. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not create folders only for superficial symmetry, such as putting every small class, interface, or helper in its own folder. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not enforce a universal maximum for functions, files, or pull requests; domain complexity, cohesion, control flow, and the number of reasons to change are more meaningful than a line count. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not pad files up to the 500-line boundary; it is a maintainability constraint, not a target. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not scatter one simple operation across many microscopic functions. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not split a file solely to make it shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not turn a green-looking diff into an unverified claim. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not use comments to narrate code that could be made clear through structure and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Avoid adding a new dependency when the need can be met without one, and avoid boilerplate that nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MAY

- Existing projects may opt out of individual strict TypeScript checks during migration, provided that choice is made explicit. _[from `com.github.haexmas.atoms.coding-guideline-javascript-typescript/javascript-typescript`]_
- A clone may be used when it expresses a real ownership boundary or is demonstrably the clearest and sufficiently cheap choice. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- `unwrap()` or `expect()` may be used when an invariant is local, explicit, and genuinely impossible to violate; document that invariant at the call site. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust`]_
- For module-private unit tests, a small declaration such as `#[cfg(test)] mod tests;` may remain in the module under test, with the test implementation kept in the separate test file. _[from `com.github.haexmas.atoms.coding-guideline-rust/rust-testing`]_
- A language-required test-module declaration or registration may remain in the production file. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Generated files, vendored code, snapshots, and other files not normally edited by hand may exceed the 500 LoC boundary. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Skip the context-map query only for a local one-file edit at a symbol whose location is already known from earlier in the same task; any other exploration, including a first look for a symbol or "where is X defined", should default to a graphify query before a raw grep, and authoring a new named artifact still requires the graphify-first-authoring consultation even when a context map would be unnecessary. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_
- The operator may suspend the graphify-first-authoring consultation rule for the current session with any natural-language request to that effect; the suspension applies only to that session, must be re-issued in a new session to take effect again, and does not exempt the agent from noting afterward which artifacts it would have flagged had the rule been active. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
