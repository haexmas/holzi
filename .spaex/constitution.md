<!-- spaex-composed:source_hash="11ebd5977a6d5746dc9e17d1464438a9105fb044c158f3e958dd12ec66805cfd" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- All changes to an existing Git repository MUST be authored in a dedicated repository-local worktree on a topic branch, applying to source code, tests, documentation, configuration, and generated files that are part of the change. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test code MUST be maintained in files separate from production code in every language, covering unit, integration, contract, and end-to-end tests; a language-required test-module declaration or registration may remain in the production file, but test bodies and test-only helpers MUST live in dedicated test files, with no inline test modules or test functions in production files, following the repository's convention for test-file placement and naming. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Treat 500 LoC as the upper boundary for hand-maintained source files: a file that is clearly over 500 LoC MUST be split or have an explicit, documented reason to remain large together with a concrete plan for a later split, since the boundary is a maintainability constraint and not a target, so files should not be padded up to 500 lines. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before authoring any new named function, class, component, store, module, or CLI command (not for renames, in-place edits, trivial inline expressions, or code being deleted), classify the checkout as a linked worktree, feature branch, or tracked branch and bootstrap or refresh the graphify graph accordingly: on a tracked branch, run `graphify update <repo-root>` when `graphify-out/graph.json` is absent or `graphify-out/.meta.json` is missing, invalid, or stale relative to `HEAD`, even if git hooks are bypassed; a linked worktree or feature branch instead uses its complete fork-point snapshot as-is; then query the graph with `graphify query`/`path`/`explain`, evaluate every candidate including unexported or unreachable ones, propose extending a clearly matching candidate (candidate location, proposed extended signature, estimated lines saved, and one proof-of-concept call-site rewrite) rather than duplicating it, escalate borderline cases to the operator instead of deciding autonomously, and author independently only when candidates are clearly unrelated; if bootstrap, refresh, or a graph query fails, warn, proceed with authoring rather than blocking on it, and flag the skipped consultation for later review; the operator may suspend this rule for the current session with any natural-language request, though suspension does not persist across sessions and does not exempt the agent from noting which artifacts it would otherwise have flagged. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Before writing any code, work through the reuse ladder: check whether it needs to be built at all, whether it already exists in the codebase, whether the standard library covers it, whether a native platform feature covers it, whether an already-installed dependency solves it, or whether it can be a one-liner, and only then write the minimum code that works. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Leave one runnable check behind for non-trivial logic, the smallest thing that would fail if the logic breaks, using whatever test framework the project has already adopted, falling back to a framework-free assert-based demo or one small test file only when no such framework exists, and skipping a test only for trivial one-liners. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark a deliberate simplification that cuts a real corner, such as a global lock, an O(n²) scan, or a naive heuristic, with a `ponytail:` comment naming the ceiling and the upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Understand the problem before climbing the reuse ladder: read the task and the code it touches and trace the real flow end to end. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When fixing a bug, find and fix the root cause in the shared function rather than only the call site the report names, by grepping every caller of the function you touch. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Keep async code non-blocking by performing blocking file, process, or CPU-heavy work through the runtime's blocking facility or a dedicated worker instead of directly on an async executor thread. _[from `com.github.haexmas.atoms.rust/rust`]_
- Keep unsafe code isolated and justified: wrap it in a safe abstraction around a small unsafe block, document the safety argument, and add a test that would expose a violation of the assumed invariant. _[from `com.github.haexmas.atoms.rust/rust`]_
- Use `Result<T, E>` for recoverable failures, propagate errors with `?`, and add context at a boundary where it helps the caller understand what failed. _[from `com.github.haexmas.atoms.rust/rust`]_
- Before handing off a change, run `cargo fmt --check`, `cargo check`, `cargo clippy`, and `cargo test` for the complete package or workspace, following the project's documented command variants where they differ, covering unit, integration, and documentation tests by default, and respect the repository's configured Clippy lint levels. _[from `com.github.haexmas.atoms.rust/rust`, `com.github.haexmas.atoms.rust/rust-testing`]_
- Cover boundaries and failure behavior deliberately: empty and malformed input, relevant limits, authorization decisions, persistence failures, and cancellation or concurrency behavior where applicable. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- For async code, use the async test support of the runtime already used by the project, and test cancellation, timeout, and ordering assumptions when they affect correctness. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- Keep tests deterministic and isolated, injecting clocks, ports, and external clients where the behavior needs control. _[from `com.github.haexmas.atoms.rust/rust-testing`]_

## MUST_NOT

- LLM agents MUST NOT leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts, including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, metadata, and other repository content. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Do not cut lazy corners on understanding the problem, input validation at trust boundaries, error handling that prevents data loss, security, accessibility, the calibration real hardware needs, or anything explicitly requested. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not write boilerplate that nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not introduce a new dependency or crate when it can be avoided and it does not solve a concrete problem the existing code cannot. _[from `com.github.haexmas.atoms.ponytail/ponytail`, `com.github.haexmas.atoms.rust/rust`]_
- Do not introduce a speculative abstraction, or a new abstraction or async strategy, merely because a change might be reusable and without a concrete problem the existing code cannot solve. _[from `com.github.haexmas.atoms.ponytail/ponytail`, `com.github.haexmas.atoms.rust/rust`]_
- Do not add `.clone()` merely to silence the borrow checker. _[from `com.github.haexmas.atoms.rust/rust`]_
- Do not create a new runtime inside an already-running async context. _[from `com.github.haexmas.atoms.rust/rust`]_
- Do not discard the original error or replace it with an uninformative string. _[from `com.github.haexmas.atoms.rust/rust`]_
- Do not encode a closed set of states or an explicitly optional value as sentinel values or loosely related booleans. _[from `com.github.haexmas.atoms.rust/rust`]_
- Do not use `unwrap()` or `expect()` on data, I/O, configuration, or network input. _[from `com.github.haexmas.atoms.rust/rust`]_
- Do not chase a coverage percentage at the expense of meaningful assertions. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- Do not depend on test order, shared mutable state, wall-clock timing, network services, or a developer-specific filesystem. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- Do not use arbitrary sleeps to wait for a task. _[from `com.github.haexmas.atoms.rust/rust-testing`]_

## SHOULD

- A file SHOULD have one primary reason to change: split a file when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate, but do not split solely to make files shorter. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments SHOULD explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies, rather than narrating code that could be made clear through structure and naming, and should be updated when the behavior changes. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any codebase question, exploratory or otherwise, run a bounded `graphify query "<intent>" --budget <N>` (or `graphify path`/`graphify explain` for a single relationship or symbol) before a raw grep/rg search or broad file reads, starting with an 800-1500 token budget and increasing it only if needed, then read only the smallest source slices needed to answer the task; if the result is truncated or ambiguous, narrow the question or make one additional focused query rather than falling back to a full graph or repository dump; the only exemption is a local one-file edit at a symbol whose location the agent already has from earlier in the same task. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_
- In JavaScript/TypeScript code, treat `strict` mode as the baseline for new or actively maintained TypeScript (existing projects may opt out of individual checks during migration if that choice is explicit), avoid `any` in favor of precise types/generics/`unknown` with narrowing, validate data at trust boundaries instead of relying on type assertions, prefer `const` with narrow local mutation, use `===`/`!==`/`??`/`?.` deliberately instead of `||` when a valid falsy value must be preserved, keep async control flow explicit (await promises that matter, handle errors at the boundary that can act on them, use `Promise.all` only for independent fail-fast work, never leave promises unhandled), preserve error causes rather than catch-log-rethrowing unchanged, keep modules and exports focused without unnecessary barrel exports, prefer immutable transformations at shared boundaries while allowing clear local mutation, follow the project's existing runtime and module conventions, and use the repository's configured formatter, linter, compiler, and test runner, with TypeScript changes at minimum passing `tsc --noEmit` (or the project equivalent), linting, formatting, and the relevant tests. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- When testing JavaScript/TypeScript code, test observable behavior and public contracts rather than private implementation details, choose the narrowest test level that proves the behavior, keep tests deterministic and isolated by controlling time/randomness/environment/external clients at explicit boundaries, cover both success and failure paths (malformed input, missing values, rejected promises, timeouts, authorization/persistence failures), await every promise that affects an assertion, keep mocks narrow and behavior-oriented rather than mocking a unit's own logic, keep type-level tests separate from runtime tests, and use the repository's configured runner, environment, formatter, linter, and compiler so a change includes relevant tests and passes the normal test command. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Prefer deletion over addition, boring solutions over clever ones, and the fewest files possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer the shortest working diff once the problem is understood; a small change in the wrong place is a second bug, not laziness. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests by asking whether a simpler existing capability already covers the need. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When two standard-library approaches are the same size, pick the edge-case-correct one, since laziness means less code, not a flimsier algorithm. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Choose the smallest correct implementation that fits the existing crate conventions. _[from `com.github.haexmas.atoms.rust/rust`]_
- Keep public APIs narrow, document public items when the crate's lint policy requires it, and avoid exposing implementation details solely to work around a local borrow or lifetime problem. _[from `com.github.haexmas.atoms.rust/rust`]_
- Prefer iterators and combinators when they make a transformation clearer, but use an ordinary loop when it communicates control flow or error handling better, since idiomatic syntax is not a reason to make code harder to read. _[from `com.github.haexmas.atoms.rust/rust`]_
- Prefer types that make invalid states unrepresentable, using enums for a closed set of states and `Option<T>` for an explicitly optional value. _[from `com.github.haexmas.atoms.rust/rust`]_
- Respect ownership instead of fighting it: borrow when the callee does not need ownership, and return owned data when the result must outlive the input. _[from `com.github.haexmas.atoms.rust/rust`]_
- Keep test helpers small and local to the tests that own them; a shared fixture is justified only when it expresses a stable domain setup, not merely to remove a few repeated lines. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- Prefer explicit assertions with useful failure context. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- Put unit-test implementations in dedicated files separate from production code, using integration tests under `tests/` to exercise the public API and documentation tests for public examples. _[from `com.github.haexmas.atoms.rust/rust-testing`]_
- Test behavior and contracts, including important failure paths, rather than mirroring private implementation details, so a test fails when the promised behavior regresses. _[from `com.github.haexmas.atoms.rust/rust-testing`]_

## SHOULD_NOT

- Avoid weakening a production invariant merely to make a test convenient; make the fixture satisfy the same contract as a real caller. _[from `com.github.haexmas.atoms.rust/rust-testing`]_

## MAY

- Generated files, vendored code, snapshots, and other files not normally edited by hand MAY exceed the 500 LoC boundary for hand-maintained source files, though they should be excluded from size assessments. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Introduce a concrete, domain-owned abstraction, even when the task did not name the extraction, when it improves cohesion, removes real duplication, or makes a branch independently testable, keeping it as small as possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Use `.clone()` when it expresses a real ownership boundary or is demonstrably the clearest and sufficiently cheap choice. _[from `com.github.haexmas.atoms.rust/rust`]_
- Use `unwrap()` or `expect()` when the invariant is local, explicit, and genuinely impossible to violate, documenting that invariant at the call site. _[from `com.github.haexmas.atoms.rust/rust`]_
